export default `
  # phone takes an optional length parameter as an argument. If length is defined 1 item is returned
  Phone_ (length: Int): Phone_
  PhoneList_ (length: Int!, locale: String): [Phone_]
`;

