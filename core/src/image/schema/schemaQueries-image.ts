export default `
  # image takes an optional length parameter as an argument. If length is defined 1 item is returned
  Image_ (locale: String): Image_
  ImageList_ (length: Int!, locale: String): [Image_]
`;

